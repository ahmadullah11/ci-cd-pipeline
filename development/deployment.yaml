apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-node-app
  namespace: development
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-node-app
  template:
    metadata:
      labels:
        app: my-node-app
    spec:
      containers:
      - name: my-node-app
        image: my-node-app:1.0
        imagePullPolicy: IfNotPresent
        envFrom:
          - configMapRef:
              name: my-node-app-config
        ports:
        - containerPort: 3000